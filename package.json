{
  "name": "last-mile-kanban",
  "private": true,
  "type": "module",
  "description": "Kanban local — servidor estático opcional",
  "scripts": {
    "start": "npx --yes serve . -l 8765",
    "test": "node --test tests/*.test.mjs",
    "build-backlog": "node scripts/build-backlog.mjs"
  }
}
